#include <stdio.h>

int fact(int no)
{
    if(no==0)
        return 1;
    else
        return no*fact(no-1);
}

int main()
{
    int x,res;
    printf("Enter number\n");
    scanf("%d",&x);
    res=fact(x);
    printf("Result is %d",res);
    return 0;
}
